import { Injectable} from '@angular/core';
import { Image } from '/src/app/models';

@Injectable()
export class ImageService {
  images: Image[] = [];

  constructor() {

  }
  
  create(image: Image) {
    
  }
  
  update(image: Image) {
    
  } 
  
  delete(image: Image) {
    
  }
}