.image-bar
  .box
    .imageBox.placeholder
      .add +
      .text drag image here
    .imageBox(*ngFor='let image of images')
      .thumbnail([ngStyle]='{"background-image": "url(" + getImage(image) + ")"}')
      .text {{ image.name }}
