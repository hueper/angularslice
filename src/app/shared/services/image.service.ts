import { Injectable} from '@angular/core';

import { Image } from '../models';

@Injectable()
export class ImageService {
  images: Image[] = [];

  constructor() {

  }
  
  create(image: Image) {
    
  }
  
  update(image: Image) {
    
  } 
  
  delete(image: Image) {
    
  }
}